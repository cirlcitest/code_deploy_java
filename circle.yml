machine:
  pre:
  - date
  - pwd
  java:
    version: oraclejdk8
   

#dependencies:
  # override:
    # -mvn clean install

test:
 pre: 
 - pwd
 - ls -l /home/ubuntu/test
 override:
    - mvn clean install
 