machine:
  pre:
  - date
  - pwd
  java:
    version: oraclejdk8
  environment:
    propfilepath: /home/ubuntu/test

#dependencies:
  # override:
    # -mvn clean install
    
dependencies:
  cache_directories:
    - "assets/cache"    # relative to the build directory
   

test:

 pre: 
 - echo $CIRCLE_USERNAME
 - echo $CIRCLE_REPOSITORY_URL
 - echo $CIRCLE_BRANCH
 - pwd
 - ls -l /home/ubuntu/test
 override:
    - mvn clean install -Dpropfilepath="/home/ubuntu/test"
    
 
 