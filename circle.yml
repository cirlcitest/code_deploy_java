machine:
  pre:
  - date
  - pwd
  java:
    version: oraclejdk8
   

#dependencies:
  # override:
    # -mvn clean install
    
dependencies:
  cache_directories:
    - "assets/cache"    # relative to the build directory
   

test:
 environment:
    propfilepath: /home/ubuntu/test
 pre: 
 
 - pwd
 - ls -l /home/ubuntu/test
 override:
    - mvn clean install -Dpropfilepath="/home/ubuntu/test/cloud_cr3.properties"
    
 
 