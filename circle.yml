machine:
  pre:
  - date
  - pwd
  java:
    version: oraclejdk8
   

#dependencies:
  # override:
    # -mvn clean install

test:
 environment:
    propfilepath: /home/ubuntu/test
 pre: 
 
 - pwd
 - ls -l /home/ubuntu/test
 override:
    - mvn clean install
    - mvn integration-test
 
 